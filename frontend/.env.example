# Leave empty in local dev — Vite's dev server proxies /api, /uploads and
# /socket.io to the backend (see vite.config.ts), so requests are same-origin.
# In production, set this to the backend's origin, e.g. https://api.example.com
VITE_API_BASE_URL=
